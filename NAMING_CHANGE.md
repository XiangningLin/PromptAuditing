# 🔄 环境变量命名变更说明
# Environment Variable Naming Change

## 📢 重要更新

为了更准确地反映项目功能，我们更新了环境变量的命名。

---

## 🆕 新命名（推荐）

```bash
ZZZ_API_KEY=your_api_key_here
ZZZ_BASE_URL=https://api.zhizengzeng.com/v1/
```

**ZZZ** = **Z**hi**Z**eng**Z**eng（智增增）

---

## 🔙 旧命名（仍然支持）

```bash
OPENAI_API_KEY=your_api_key_here
OPENAI_BASE_URL=https://api.zhizengzeng.com/v1/
```

---

## 🤔 为什么要改名？

### 问题：旧命名容易误导

使用 `OPENAI_API_KEY` 会让人误以为：
- ❌ 只支持 OpenAI 的模型
- ❌ 这是 OpenAI 官方的 API 密钥
- ❌ 不能使用其他提供商的模型

### 事实：支持 34+ 个不同提供商

本项目实际支持：
- ✅ **OpenAI** (6 models): GPT-4o, GPT-3.5 Turbo...
- ✅ **Anthropic** (4 models): Claude 3.5 Sonnet, Claude 3 Opus...
- ✅ **Google** (3 models): Gemini 1.5 Pro, Gemini Flash...
- ✅ **阿里巴巴** (4 models): 通义千问 Max, Plus, Turbo...
- ✅ **百度** (3 models): 文心一言 4.0...
- ✅ **智谱** (3 models): ChatGLM-4...
- ✅ **DeepSeek** (2 models): DeepSeek Chat, Coder...
- ✅ **字节跳动** (2 models): 豆包 Pro, Lite...
- ✅ **百川** (2 models): 百川2 Turbo...
- ✅ **讯飞** (2 models): 星火 3.5, 3.0...
- ✅ **xAI** (1 model): Grok Beta
- ✅ **Meta** (2 models): Llama 3 70B, 8B

**一个密钥，34+ 个模型！**

### 解决方案：使用 ZZZ_API_KEY

新命名的优势：
- ✅ 明确表示这是智增增（Zhizengzeng）的统一 API 密钥
- ✅ 避免与 OpenAI 官方 API 混淆
- ✅ 准确反映支持多个 AI 提供商的事实
- ✅ 简洁易记（ZZZ）

---

## 🔄 迁移指南

### 不需要立即迁移！

**重要**：旧命名 `OPENAI_API_KEY` 仍然完全支持，你的现有配置会继续工作。

### 如果你想迁移到新命名

#### 步骤 1：更新 .env 文件

**旧的 .env 文件：**
```bash
OPENAI_API_KEY=sk-zk2a256993d569634deabbbcd007279f33f690e5aef05532
OPENAI_BASE_URL=https://api.zhizengzeng.com/v1/
```

**新的 .env 文件（推荐）：**
```bash
ZZZ_API_KEY=sk-zk2a256993d569634deabbbcd007279f33f690e5aef05532
ZZZ_BASE_URL=https://api.zhizengzeng.com/v1/
```

#### 步骤 2：测试配置

```bash
python3 test_api.py
```

看到 ✅ 就成功了！

#### 步骤 3：删除旧变量（可选）

确认新配置工作后，可以从 `.env` 文件中删除旧的变量名。

---

## 🔍 技术细节

### 优先级顺序

代码会按以下顺序查找环境变量：

```python
# 1. 优先使用新命名
api_key = os.environ.get("ZZZ_API_KEY")

# 2. 如果没有，尝试旧命名（向后兼容）
if not api_key:
    api_key = os.environ.get("OPENAI_API_KEY")

# 3. 都没有则报错
if not api_key:
    raise ValueError("API key not found")
```

### 实现方式

在所有 Python 文件中：

```python
# 优先使用 ZZZ_API_KEY，向后兼容 OPENAI_API_KEY
api_key = os.environ.get("ZZZ_API_KEY") or os.environ.get("OPENAI_API_KEY")
base_url = os.environ.get("ZZZ_BASE_URL") or os.environ.get("OPENAI_BASE_URL", "https://api.zhizengzeng.com/v1/")
```

### 受影响的文件

以下文件已更新以支持新命名：
- ✅ `app.py` - 主应用
- ✅ `benchmark.py` - Benchmark 系统
- ✅ `test_api.py` - API 测试脚本
- ✅ 所有文档文件

---

## 📋 对比表

| 特性 | 旧命名 | 新命名 |
|------|--------|--------|
| **变量名** | `OPENAI_API_KEY` | `ZZZ_API_KEY` |
| **清晰度** | ❌ 容易误导 | ✅ 准确清晰 |
| **支持状态** | ✅ 向后兼容 | ✅ 推荐使用 |
| **优先级** | 第二优先 | 第一优先 |
| **功能** | 完全相同 | 完全相同 |

---

## ❓ 常见问题

### Q1: 必须立即迁移吗？
**A:** 不必须。旧命名 `OPENAI_API_KEY` 会一直支持，不会被移除。

### Q2: 如果同时设置了两个会怎样？
**A:** 会优先使用 `ZZZ_API_KEY`，忽略 `OPENAI_API_KEY`。

### Q3: 为什么选择 ZZZ 作为前缀？
**A:** 
- ZZZ 是智增增（**Z**hi**Z**eng**Z**eng）的缩写
- 简洁易记
- 避免与其他服务混淆
- 明确表示这是智增增的统一 API

### Q4: 会影响现有的脚本吗？
**A:** 不会。所有代码都已更新为同时支持两种命名方式。

### Q5: 其他项目也需要改吗？
**A:** 不需要。这只影响本项目。如果你在其他地方使用智增增 API，可以继续使用任何你喜欢的变量名。

### Q6: 文档都更新了吗？
**A:** 是的。所有文档都已更新，推荐使用新命名，同时说明旧命名仍然有效。

---

## 📊 使用建议

### 新项目
```bash
# 推荐：使用新命名
ZZZ_API_KEY=your_key
ZZZ_BASE_URL=https://api.zhizengzeng.com/v1/
```

### 现有项目
```bash
# 选项 1：继续使用旧命名（完全可以）
OPENAI_API_KEY=your_key

# 选项 2：迁移到新命名（推荐）
ZZZ_API_KEY=your_key

# 选项 3：同时保留（会优先使用 ZZZ_API_KEY）
ZZZ_API_KEY=your_key
OPENAI_API_KEY=your_key  # 作为备份
```

---

## 🎯 总结

### 核心要点

1. ✅ **新命名更准确**：`ZZZ_API_KEY` 反映支持 34+ 个提供商
2. ✅ **完全向后兼容**：`OPENAI_API_KEY` 仍然有效
3. ✅ **无需强制迁移**：可以继续使用旧命名
4. ✅ **推荐新项目使用**：`ZZZ_API_KEY` 避免误解

### 迁移时间线

- **现在**：两种命名都支持
- **未来**：两种命名会持续支持
- **建议**：新项目使用 `ZZZ_API_KEY`，旧项目可选择迁移

### 快速命令

```bash
# 创建新配置（推荐）
cat > .env << EOF
ZZZ_API_KEY=sk-zk2a256993d569634deabbbcd007279f33f690e5aef05532
ZZZ_BASE_URL=https://api.zhizengzeng.com/v1/
EOF

# 测试配置
python3 test_api.py

# 查看当前配置
echo "ZZZ_API_KEY: $ZZZ_API_KEY"
echo "OPENAI_API_KEY: $OPENAI_API_KEY"
```

---

## 📚 相关文档

- [ENV_EXAMPLE.md](ENV_EXAMPLE.md) - 环境变量配置详细示例
- [START_HERE.md](START_HERE.md) - 快速开始指南
- [QUICK_START.md](QUICK_START.md) - 完整快速开始
- [API_CONFIG.md](API_CONFIG.md) - API 配置说明

---

**感谢你的理解和支持！新命名让项目更清晰、更准确。** 🎉

**记住**：`ZZZ_API_KEY` = 智增增统一 API 密钥 = 34+ 个模型 = 一个密钥全部搞定！🚀

